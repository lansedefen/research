#include <iostream>
#include <vector>

using namespace std;

void adjust_heap(vector<int> & heap, int value) {
	head[0] = value;
	int i = 0;
        while(i) {
	   if (heap[i] < ) 
	       i = i*2;	   
	}
}	

int main() {
   vector<int> tmp;
   tmp.push_back(-1);   
   tmp.push_back(10);   
   tmp.push_back(3);   
   tmp.push_back(13);   
   tmp.push_back(-3);   

   for (int i = 0;i < tmp.size(); i++) {
      cout << tmp[i] << endl;	  
   }
}
